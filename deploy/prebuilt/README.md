# Pay3 Prebuilt Binary

This directory is the Docker build context input for `Dockerfile.prebuilt`.

Generate the binary with:

```bash
scripts/build-prebuilt-binary.sh
```

The generated `pay3` binary is intentionally ignored by git.
